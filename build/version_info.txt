# UTF-8
#
# Windows EXE version-info resource for PyInstaller's EXE(version=...).
# Populates the "Details" tab of the .exe's Properties dialog in Explorer.
#
# Keep filevers/prodvers in sync with APP_VERSION in DARKSKY_NEXUS_Windows.spec
# (and build_Windows.bat) — bump all three together on every release.

VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=(0, 3, 3, 0),
    prodvers=(0, 3, 3, 0),
    mask=0x3f,
    flags=0x0,
    OS=0x4,        # VOS_NT_WINDOWS32
    fileType=0x1,  # VFT_APP
    subtype=0x0,
    date=(0, 0)
  ),
  kids=[
    StringFileInfo(
      [
      StringTable(
        u'040904B0',
        [StringStruct(u'CompanyName', u'Jon Nicol'),
        StringStruct(u'FileDescription', u'DARKSKY NEXUS — SDRConnect Companion'),
        StringStruct(u'FileVersion', u'0.3.3.0'),
        StringStruct(u'InternalName', u'DARKSKY NEXUS w033 Windows'),
        StringStruct(u'LegalCopyright', u'© 2025 Jon Nicol & Claude / Anthropic — Freeware, personal & educational use.'),
        StringStruct(u'OriginalFilename', u'DARKSKY NEXUS w033 Windows.exe'),
        StringStruct(u'ProductName', u'DARKSKY NEXUS'),
        StringStruct(u'ProductVersion', u'0.3.3.0')])
      ]),
    VarFileInfo([VarStruct(u'Translation', [1033, 1200])])
  ]
)
